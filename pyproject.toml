[project]
name = "RuntimeIQ"
version = "0.1.0"
description = "AI orchestration and execution engine"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.141.1",
    "psycopg2>=2.9.12",
    "redis>=8.1.0",
    "requests>=2.34.2",
]
